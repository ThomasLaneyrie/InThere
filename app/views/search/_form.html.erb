<%= search_form_for(@q, url: search_path, method: :get, class:"d-flex") do |f| %>
  <%= f.search_field :description_cont, placeholder: "recherchez dans la description", class: "form-control me-2" %>
  <%= f.submit "Ok !" %>
<% end %>
