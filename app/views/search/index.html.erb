<center>
	<div class="container my-5">
		<div class="row">
			<div class="bs-component">
  			<section class="items-section">
  				<br>
    		<h3 class= "text-center" ><%= pluralize(@products2.count, "Resultat(s) trouvé(s)") %> </h3>
				<%= render "search/form" %>

			<section class="items-section">
				<div class="row">
			<% @products2.each do |product| %>
						<%= render product %>
					<% end %>
				</div>
			</section>
		</div>
	</div>