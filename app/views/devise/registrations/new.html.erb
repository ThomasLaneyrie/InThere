<div class="bs-component">
  <div class="container mt-4", style="max-width:600px">  
    <h2 >S'inscrire</h2>
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>

      <div class="form-label mt-4">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true, autocomplete: "email", class:"form-control" %>
      </div>

      <div class="form-label mt-4">
        <%= f.label :password %>
        <% if @minimum_password_length %>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "new-password", placeholder:"#{@minimum_password_length} caractères mini", class:"form-control"  %>
      </div>

      <div class="form-label mt-4">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "new-password", placeholder:"#{@minimum_password_length} caractères mini", class:"form-control"%>
      </div>
  </div>

  <div class="container mt-2", style="max-width:600px"> 
    <div class="row">
      <div class="actions">
        <%= f.submit "Inscription !", class:"btn btn-outline4 btn-lg mt-2 w-100" %>
      </div>
    </div>
    <div class="row mt-4">
      <%= render "devise/shared/links" %>
    </div>
    </div>
    <% end %>
  </div>
</div>

